# URL do backend NestJS rodando localmente
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# MESMO valor de API_TOKEN que você colocou em api/.env
API_TOKEN=lucrom-dev-secret-2026

# MESMO valor de JWT_SECRET que você colocou em api/.env
JWT_SECRET=jwt-secret-dev-2026

# Opcional — mostra a aba Arquitetura no menu
NEXT_PUBLIC_SHOW_INTERNAL_VIEWS=false
